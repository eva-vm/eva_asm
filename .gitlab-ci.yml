default:
  image: ocaml/opam

stages:
  - build
  - test

build:
  stage: build
  script:
    - echo "[Building]"
    - opam pin add ./
    - opam install eva_asm

test:
  stage: test
  script:
    - echo "[Testing]"
    - dune runtest
  
